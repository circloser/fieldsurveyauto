# 로컬 개발용 시크릿 예시 파일.
# 실제 값은 이 파일을 복사해 ".dev.vars" 로 저장해 넣으세요. (.dev.vars 는 깃에 안 올라감)
# 운영(배포) 환경에서는 이 파일이 아니라 `wrangler secret put` 으로 넣습니다.

# 진짜 Claude API 키 (팀 계정) — 서버에만 존재
ANTHROPIC_API_KEY=sk-ant-여기에-진짜-키

# exe 가 프록시에 인증할 앱 토큰 (아무 긴 랜덤 문자열. 유출 시 교체)
APP_TOKEN=여기에-긴-랜덤-문자열
