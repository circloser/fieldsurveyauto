# AI 자동추출 설정 — 이 파일을 exe(FieldSurveyDB.exe)와 같은 폴더에 'ai_config.txt' 로 둡니다.
# 각 사용자가 '본인의 Claude(Anthropic) API 키'를 넣으면, 본인 사용량으로 AI 기능이 켜집니다.

# ▶ 본인 Anthropic API 키 (발급: https://console.anthropic.com → API Keys)
ANTHROPIC_API_KEY=sk-ant-여기에-본인-키-붙여넣기

# 선택: 사용할 모델(기본 claude-opus-4-8). 비용을 낮추려면 하이쿠 등으로 교체.
# FIELD_SURVEY_VISION_MODEL=claude-opus-4-8

# ─────────────────────────────────────────────────────────────
# (선택·고급) 회사 공용 프록시를 쓰는 경우엔, 위 ANTHROPIC_API_KEY 대신 아래 2개만 채우세요.
# 이러면 진짜 키는 서버에만 있고 각 PC엔 통행증(APP_TOKEN)만 둡니다.
# FIELD_SURVEY_PROXY_URL=https://field-survey-claude-proxy.○○○.workers.dev
# FIELD_SURVEY_APP_TOKEN=앱토큰
