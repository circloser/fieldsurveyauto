# 진행 현황 (현장 조사표 DB화)

## 완료 (검증됨 · 테스트 31개 통과)
- **Phase 0** 원클릭 실행 뼈대 (`start.bat`, 포트 자동탐색, 브라우저 자동열기)
- **Phase 1** hwpx 파싱 엔진 (병합셀 좌표맵, 런분할 R9, 정규화 R10)
- **Phase 2** 서식 자동판별(A/C/D/E) + 다중 보 분리
- **Phase 3** A(인공구조물)·C(어도) 값 추출 + 체크(√) 감지
- **Phase 4** 엑셀 생성 (요약 + 서식별 시트 + 검수 강조)
- **Phase 5** 웹 화면 (드래그 업로드 · 대시보드 · 다운로드)
- **Phase 6** 검수·수정 (화면에서 값 편집 → 자동저장 → 엑셀 반영, 안정 키 R11)
- **Phase 7(부분)** 어류(D) 종명/개체수 목록 추출 + 어류 시트

## 현재 추출 가능 서식
- ✅ A 인공구조물 (제원·재질·용도 등)
- ✅ C 어도 (제원·어도유형·물흐름)
- ✅ D 어류 (종명·개체수 목록)
- ⬜ E 횡적연속성 (여러 표에 걸쳐 있어 미구현 — 다음)
- ⬜ B 대표지점 (식별정보만)

## 🖼️ PDF 통합 + 픽셀박스 디자이너 (현재 주력 방향)
hwpx·PDF를 **모두 PDF로 통일**해 위치 기반으로 처리. 화면은 **실제 문서 이미지** 그대로.
- hwpx → PDF 변환: 한글(pyhwpx). 첫 파일 ~20초, 이후 빠름. (한글 설치 필요)
- PDF 읽기(pymupdf): 글자+위치+굵게 추출, 텍스트 PDF는 OCR 불필요
- `/pdf-designer`: PDF 페이지 이미지 위에 **픽셀 박스 드래그**, 자동제안(라벨 앵커)
- 라벨 앵커(하천명 오른쪽 등) → hwpx·PDF·양식편차 공통 대응, 검증 7/7 정확
- 일괄 처리 → 박스 순서대로 엑셀. 엔드투엔드 검증 완료.
- ✅ 스캔(이미지) PDF OCR: EasyOCR(한국어). read_pdf가 텍스트 없는 페이지를 자동 OCR → 같은 박스/앵커로 추출. 검증: 이미지PDF→OCR→하천명→해남천 정확(위도 349 등 OCR 오차는 검수 보완). OCR은 선택 설치(requirements-ocr.txt, 용량 큼).
- 자동제안: PDF 변환 단어배치로는 라벨/값 판정이 불안정 → **기본 끔(깨끗한 문서)**, 사용자가 직접 드래그. "✨ 자동 제안 시도"(부정확·데이터값만) / "🗑 전체 지우기" 버튼 제공. 정렬(좌표)은 정확 확인(오프셋 없음).
- 남은 것: 배포 패키징(한글·OCR 안내 포함)
- 참고: 기존 칸 기반 디자이너(`/designer`)는 유지되나 주력은 PDF 방식으로 이동

## 🧩 (구) 칸 기반 템플릿 디자이너 (프로토타입)
코드에 서식을 박아넣는 대신, **사용자가 화면에서 추출 항목을 직접 지정**하는 범용 방식.
- `/designer` 페이지: 양식 hwpx 불러오기 → 표 그대로 격자 재현
- **자동 박스 제안** (라벨→값 칸 감지, 84개 제안)
- 박스 편집: 칸 클릭으로 추가, 이름·순서 변경, 삭제
- 템플릿 저장 → 같은 양식 여러 파일 **일괄 처리** → 박스 순서대로 엑셀
- 브라우저 엔드투엔드 검증 완료(로드/제안/편집/적용/다운로드)
- 방식: 칸(셀) 기반. PDF 픽셀 박스는 다음 단계.

## 🤖 AI(Vision) 추출 — 경진대회 방향 (신규, Phase 1)
`보이는 대로 스키마만 정하고, 페이지 이미지를 Claude Vision이 통째로 읽어 값 추출`. 밀림·제목변경·스캔에 강건.
- **키 보안**: 진짜 Claude 키는 Cloudflare Worker 프록시(`claude-proxy/`)에만. 클라이언트는 프록시 주소+앱토큰만 사용. 배포·가드(모델화이트리스트·토큰상한·레이트리밋) 검증 완료.
- **엔드투엔드 실증**: `sample.pdf`(서식 A) → 프록시 → Vision → **24/24 항목 정확 추출** 확인. (하천명 해남천 등 기존 정답과 일치, 체크박스 재질/용도도 정확)
- **강건화 뼈대**(독립 모듈, 파이프라인 미연결):
  - `core/layout.py` — 퍼지 라벨(자모단위 OCR오타 관대) + 상대앵커(offset) → 틀어짐 대응
  - `core/layout_fingerprint.py` — 배치 지문 매칭(제목 제외) → 제목 바뀐 양식 대응
  - `core/vision_extract.py` + `core/extraction/schema/vision_schemas.py` — Vision 추출 + FieldSpec→JSON스키마 자동생성
  - 테스트 `tests/test_layout.py`·`tests/test_layout_fingerprint.py` (합성데이터로 강건성 증명), 실행기 `scripts/try_vision.py`
- **서식 라우터**(`core/form_router.py`·`core/bundle.py`): 페이지마다 서식 자동 판별(무료 시그니처, 제목 무관 → 애매하면 Vision 폴백) 후 알맞은 스키마로 추출. 다중서식 번들(A+B+C+사진) 한 파일 자동 처리 검증(test_3line 8p, 라우팅 8/8 정확). `detect_form_words` 추가, C 조사자 2명 분리(조사자1/2).
- **정확도 평가**(`eval/`): 검증 골드 4건(A, 2명·3명·보철거 변형) = **99% (99/100)**. 채점 코어 `core/eval_score.py`, 실행기 `eval/run_eval.py`.
- 계획 상세: `COMPETITION_PLAN.md`
- 다음: 골드셋 확장(C·스캔·제목변형) → 하이브리드(신뢰도·규칙 교차검증) → B·D·E 스키마

## 남은 작업
- **E 횡적연속성** 서식 추출 (등급·비율·면적, 다중 표)
- **텍스트 PDF** 입력 지원 (샘플 PDF 필요)
- **구버전 .hwp** 안내 처리 (한글에서 hwpx 저장 유도)
- **포터블 파이썬 패키징** (인터넷 없는 PC 대응)
- 사용자 피드백 반영 (기상상태 등 형식정리형 필드, 실제 파일 편차)

## 알려진 한계
- 기상상태 등 '볼드로 선택 표시'하는 필드는 자동 감지 안 됨 → 검수에서 수정
- 스키마는 제공된 샘플 1건 기준 → 다른 사무실 양식은 편차 가능(검수로 보완)
- 실행 순서: `install.bat`(최초 1회) → `start.bat`(매번)
