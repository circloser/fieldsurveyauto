"""B: 인공 구조물 현장 조사표 2 — 대표지점 + 조사대상 어도 (스칼라 값).

레이아웃(샘플):
  대표지점:  유폭 / 거리(좌측) / 거리(우측)
  조사대상 어도: 거리(좌측) / 거리(우측)
'거리(좌측/우측)'가 대표지점·어도 두 곳에 각각 있으므로 필드명을 구분한다
(Vision이 섹션 문맥으로 올바른 값을 매칭).

※ 하단 '형태별 물리·수리적 현황'(경사형/수직형 × 순서 × DH·Hu·Hd·U·각도)은
   중첩(반복) 구조라 이 스칼라 스키마에 미포함 — 별도 리스트형 확장 예정.
   → Vision 전용 레지스트리(vision_schemas.VISION_SCHEMAS)에만 등록해
     규칙기반 파이프라인(core.extraction.schema.SCHEMAS)에는 영향을 주지 않는다.
"""
from core.extraction.schema.spec import FieldSpec as F

SCHEMA_B = [
    F("하천명", "right", "하천명"),
    F("보명칭", "right", "구조물(보) 명칭", required=False),
    F("유폭", "right", "대표지점 유폭"),
    F("대표지점_거리_좌측", "right", "대표지점 거리(좌측)"),
    F("대표지점_거리_우측", "right", "대표지점 거리(우측)"),
    F("어도_거리_좌측", "right", "조사대상 어도 거리(좌측)"),
    F("어도_거리_우측", "right", "조사대상 어도 거리(우측)"),
]
