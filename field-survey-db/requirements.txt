# 현장조사표 DB화 - 로컬 웹앱 의존성
# 모두 무료/오픈소스. 로컬에서만 실행됩니다.
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20
openpyxl==3.1.5
pdfplumber==0.11.5
pymupdf==1.25.2
# hwpx → PDF 변환용(한글 설치된 Windows에서). PDF만 쓰면 없어도 됩니다.
pywin32==308
pyhwpx==1.7.2
